# reject(err) <sup>async</sup>

Force the currently awaited promise to be rejected with the given error

# Syntax

```js
  await reject(new HTTPError(404));
```

# Parameters

* `err` - <Error> An error object
